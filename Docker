
# Workdir
WORKDIR /app

# Copy requirements (put the final requirements as requirements.txt in repo root)
COPY requirements.txt /app/requirements.txt

# Install Python deps
RUN pip install --upgrade pip && pip install -r /app/requirements.txt

# Copy app code
COPY . /app

# Streamlit configuration
ENV STREAMLIT_SERVER_HEADLESS=true \
    STREAMLIT_BROWSER_GATHER_USAGE_STATS=false

# HF Spaces will set $PORT (default 7860). Streamlit must bind to 0.0.0.0
EXPOSE 7860

# IMPORTANT: adjust main file path if you rename/move your app
CMD streamlit run "Att project/app1.py" --server.port $PORT --server.address 0.0.0.0
